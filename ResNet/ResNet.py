from Model import Model
import numpy as np
import tensorflow as tf
from Data import data

class ResNet(Model):
    """
    Description
    """

    # 1. Read data from the downloaded dataset
    def read_data(self, data=data()):pass

    # 2. Create data_sets and iterator,it should finished in data class

    # 3. construct the structure of one network model, such as weights and bias

    # 4. Define loss_function

    # 5. Define optimizer

    # 6. Define some evaluate function such as accuracy

    # 7. Test the model on test set
